name: Run Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build whole project
        run: dotnet build ${{ inputs.projectPath }}
        
      - name: Build and run test
        run: dotnet test ${{ inputs.projectPath }}
